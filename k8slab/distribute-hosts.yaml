---
- name: push hosts config
  hosts: '{{ cluster }}'
  gather_facts: false
  tasks: 
    - name: copy hosts 
      copy: 
        src: /etc/hosts
        dest: /etc/hosts
        owner: root
        group: root
        mode: '0644'

...
